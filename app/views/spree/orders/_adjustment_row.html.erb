<% if adjustments.sum(&:amount) != 0 %>
  <div class="adjustment">
    <h5><%= type %>: <%= label %></h5>    
    <h5 style="text-align:right;"><%= Spree::Money.new(adjustments.sum(&:amount), :currency => @order.currency) %></h5>
  </div>
<% end %>
